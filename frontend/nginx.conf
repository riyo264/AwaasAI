server {
	listen 80;
	server_name _;
	root /usr/share/nginx/html;
	index index.html;

	# react-router owns the routes; unknown paths must fall back to the SPA
	# shell rather than 404.
	location / {
		try_files $uri $uri/ /index.html;
	}
}
